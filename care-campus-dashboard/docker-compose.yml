version: "3.9"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules  # Persist node_modules outside container
    env_file:
      - ./env/.env.local  # Loads your Supabase keys
    command: npm run dev