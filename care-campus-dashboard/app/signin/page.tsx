export default function SignInPage() {
    return (
        <div className="min-h-screen flex items-center justify-center bg-zinc-50 dark:bg-black">
            <div className="text-center p-8 bg-white dark:bg-zinc-900 rounded-lg shadow-xl">
                <h1 className="text-2xl font-bold text-black dark:text-zinc-50 mb-4">Sign In Required</h1>
                <p className="text-zinc-600 dark:text-zinc-400">
                    Need to implement our Supabase authentication logic here (e.g., Email/Password or OAuth).
                </p>
            </div>
        </div>
    );
}